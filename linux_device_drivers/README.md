## README
    - Device Driver comands for inserting : insmod, lsmod, rmmod 
    - how to create a entry for the driver in /proc/ filesystem 
    - and then read/write operation on the /proc file will make the communication to driver 
    - User Space to kernel space communication is facilitated 
 

 ## Linux Kernel facilities 
    - kernel provides with a many datastructures template that we can leverage to create and use user space applications 
    